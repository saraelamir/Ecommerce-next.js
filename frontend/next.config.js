/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['example.com', 'localhost', 'via.placeholder.com', 'picsum.photos', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
